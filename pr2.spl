import as DATA;                 ||Store the data passed through STDIN as a variable called DATA
print {DATA @0, DATA @0};       ||Print a new StreamBlock consisting of two streams, each of which is the first stream in DATA. 